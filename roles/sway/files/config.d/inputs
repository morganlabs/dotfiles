input type:keyboard {
  xkb_layout gb
  xkb_options caps:escape
}

input type:mouse {
  accel_profile flat
  pointer_accel -0.25
}

input type:touchpad {
  accel_profile flat
  pointer_accel 0.45
  natural_scroll enabled
  drag enabled
  drag_lock enabled
  tap enabled
}
